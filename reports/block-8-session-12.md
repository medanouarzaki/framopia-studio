Status: OK

Block 8 part 2, session 12. **$0.00 spent, no API called, After Effects not
driven.** The transliteration-aware substitution cost is adopted, the user's
re-review is committed, session 11's three unverified claims are settled, and
splits and merges are measured against the fixed aligner.

## Session preconditions

| check | reading |
|---|---|
| working directory | `/Volumes/T7 Shield/INSEA/Projects/framopia-studio` (T7 mounted; nothing read from `~/dev`) |
| `git status` at start | **no modified or staged files.** No untracked files either. |
| HEAD at start | `127162a` |
| ledger at start | **108 lines**, sha256 `50ec3f57bacb3f32054ae190d3a7652dea5e55c68d71a494fa85b2347a417593` |
| ledger at end | **108 lines**, sha256 `50ec3f57bacb3f32054ae190d3a7652dea5e55c68d71a494fa85b2347a417593` — byte-identical |
| After Effects instances at start | **1** |
| `aerender` processes at start | **0** |

No AE work was done. `correction.ts` was not touched and the prompt version
stays frozen at 4.

## Done

### Goal 1 — the user's reference is committed

**It was not in the repo.** It was still in `~/Downloads` as
`vitasilk.rereview.align-reference.json`, under the name the sheet gave it,
dated 23:04. Copied to `benchmarks/references/align/vitasilk.rereview.json`.

**Verified before committing, every check passing:**

| check | result |
|---|---|
| `schemaVersion` | 3 |
| `reel` | `vitasilk` |
| `rowCount` | 17 |
| `markedCount` | 16 |
| entries | 17 |
| `markedCount` equals non-null verdicts | **yes** (16) |
| `(wordId, wordText, draftTokenText)` triples vs the generated sheet | **identical, in order** |

Verdicts: **7 correct, 7 wrong, 2 misheard, 1 null** — matching the ruling.
`w0036` (`26`) is the deliberate null and was **not** filled in.

The README gained a table naming both files, stating both are hand-made and
never regenerated by code, and explaining why the unjudged row stays null: an
unjudged row and a row nobody was shown are different facts, and schema 3 exists
to keep them apart.

### Goal 2 — the three open questions

**(a) The test arithmetic reconciles, and session 11's report was incomplete.**

`@framopia/core` went 327 → 337. The accounting:

| file | before | after | change |
|---|---:|---:|---|
| `align-sheet.browser.test.ts` | 0 | 9 | **+9, all new** |
| `align-sheet.test.ts` | 22 | 23 | **+1 net** |
| `align-score.test.ts` | 25 | 25 | unchanged |
| `align-review.test.ts` | 13 | 13 | unchanged |

**9 + 1 = 10.** The nine new Playwright tests are `has a browser to drive`,
`writes all 17 rows, in display order, with the verdicts given`, `keeps an
unmarked row with a null verdict and counts only the marked`, `restores marks
after a reload, and says how many`, `shows a live marked count beside the
download control`, `pre-fills restored verdicts, marks them as restored, and
lets them change`, `never writes fewer entries than the rows on screen, however
they are marked`, `are migrated onto the right rows and shown as restored`,
`does not overwrite marks already made under the current key`.

The tenth is **`carries counts written by the same walk that produced the
entries`**, in `align-sheet.test.ts`. **Session 11's report never mentioned it**,
which is the whole discrepancy: it said "nine Playwright tests", which was
correct, and left the tenth unnamed.

Seven tests were rewritten, all net-zero: four renamed (`keys the store by
sheet variant…` → `keys the store by variant, reel, sha and the row set`;
`produces a file that parses against the reference schema` → `writes every
displayed row, in display order, marked or not`; `carries a note when one was
typed, and omits unjudged rows` → `carries a note, and keeps the unmarked rows
with a null verdict`; `downloads at schema 2…` → `downloads at the current
schema…`) and three rewritten under their existing names (`never presents the
aligner's pairing as a verdict`, `keys its store apart from the review sheet`,
`downloads a reference in the same shape`).

**(b) The scorer change did not move what it measures.** Part 1's comparison
re-run against the hand-made schema-1 `vitasilk.json`, 73 rows, under both
models:

| figure | part 1 recorded | re-run | |
|---|---|---|---|
| rows judged | 73 of 73 | 73 of 73 | ✓ |
| correct / wrong / two-tokens | 54 / 18 / 1 | 54 / 18 / 1 | ✓ |
| human-confirmed | 74.0% | 74.0% | ✓ |
| moved under transliteration | 16 | 16 | ✓ |
| **regressions** | **0** | **0** | ✓ |
| wrong unmoved | 2 | 2 | ✓ |
| correct held | 54 | 54 | ✓ |

**Every figure identical.** The null-verdict filter cannot change a schema-1
file, which has no nulls, and does not.

**(c) The migration claim was half right, and its conclusion was wrong.**

The generator at `37a05d8` **did** write marks to `localStorage` —
`window.localStorage.setItem(KEY, …)` at line 289 of `align-sheet.ts`, called
from `save()` on every verdict click and note edit. So session 11's premise was
true.

**But the migration could never have found them.** The key is
`'framopia.align-review.' + VARIANT + '.' + REEL + '.' + HEAD`, and `HEAD` is
the **git HEAD at sheet-generation time**:

- the sheet the user marked carried `var HEAD = "d7c46adb3b9b…"`;
- the sheet session 11 regenerated carried `var HEAD = "37a05d8458…"`;
- so his marks live under the `d7c46ad…` key, and the migration searched
  `…37a05d8…` — **a key that never existed**.

Session 11 asserted "if he opens the regenerated sheet in the same browser
profile, all 17 should reappear." That was wrong, and it could have been checked
in that session: both sheets were on disk, one of them copied to `/tmp` by that
same session. **This is the §3 failure the prompt names** — a claim about the
real environment made without evidence.

**His original marks are most likely still in his browser**, under
`framopia.align-review.rereview.vitasilk.d7c46adb3b9b6fe837deea3b9922f83d074fdbcf`.
He has since redone the work, so nothing depends on it.

The design flaw underneath is worth naming: **the persistence key embeds a sha
that changes every time the repo is committed to, so marks never survive a
regeneration of the sheet.** Not fixed here — this session's only sanctioned
behavioural change is Goal 3.

### Goal 3 — adopted

`ACTIVE_ALIGN_COST_MODEL` in `service/src/transcription/align.ts` is
`transliteration`. `alignCorrectedOntoDraft` takes an optional third argument;
nothing in the pipeline passes it.

**`legacy` stays selectable**, the flat model, in the same terms as prompt
version 2 in `correction.ts`: every figure recorded before today was measured
with it and a comparison against those numbers has to reproduce them.

Nothing else about alignment changed. No merge operation, no interpolation
change, no normalisation change.

### Goal 4 — the corpus safety check passes

From the cached Scribe responses and corrected texts, no model call, entry
`transcription-758a3924d090d1b5` on every reel, prompt version 4:

| reel | words | anchored legacy → adopted | interpolated | anchors moved | zero-duration | duplicate intervals |
|---|---:|---|---|---:|---|---|
| ground-truth | 76 | 72 → **72** | 4 → 4 | 15 | 4 → 4 | 0 → 0 |
| test-1 | 67 | 64 → **64** | 3 → 3 | 17 | 3 → 3 | 0 → 0 |
| test-2 | 69 | 68 → **68** | 1 → 1 | 14 | 1 → 1 | 0 → 0 |
| test-3 | 58 | 56 → **56** | 2 → 2 | 4 | 2 → 2 | 0 → 0 |
| vitasilk | 73 | 70 → **70** | 3 → 3 | 17 | 3 → 3 | 0 → 0 |
| **corpus** | **343** | **330 → 330** | 13 → 13 | **67** | 13 → 13 | **0 → 0** |

**No reel loses an anchored word.** The corpus total independently reproduces
Block 7's recorded 330, which is what makes the guard meaningful — Block 7's
discarded fix took it to 230.

One correction to my own method, recorded because it nearly produced a
meaningless pass: my first probe tested `confidence` for anchoredness and read
0 anchored everywhere. Raw Scribe words carry `logprob`, and only
`mapScribeResponse` converts it. The structural test is `sourceText`, which the
aligner sets only on an anchored word.

### Goal 5 — splits and merges, read-only

No code was changed in this goal.

| reel | split runs (corrected > draft) | merge runs (draft > corrected) |
|---|---:|---:|
| ground-truth | 3 | 1 |
| test-1 | 1 | 2 |
| test-2 | 1 | 1 |
| test-3 | 2 | 1 |
| vitasilk | 3 | 1 |
| **corpus** | **10** | **6** |

**Part 1's floor was low by roughly an order of magnitude**, and it measured the
wrong direction: **splits outnumber merges**, and part 1 never counted splits at
all.

Token texts, every run:

- **ground-truth splits** — `pigmentés tb3i m3aya tal lkher dial` ← `pigmentées?
  تبعي معايا للخر ديال`; `3ndi lik joj dial l7loul awal 7el houa الإبرة` ←
  `عندي ليك جوج دالحلول. أول حل هو إبرة`; `li houa wa7d lcocktail dial
  lvitaminat wzayd 3lih` ← `اللي هو واحدcocktail دالفيتامينات وزايد عليه`.
  **merge** — `yom wl'effet dialha kidom lmoddat sana l7el ttani houa` ← `يوم و
  l'effet ديالها كيدوم لمدة سنة. الحل الثاني هو`.
- **test-1 split** — `tb3i m3aya tal lkher dial la vidéo lyoma ghadi nhdr likom
  3la` ← `تبعي معايا للآخر الفيديو. اليوم غادي نهضر لكم على`. **merges** —
  `dialna wki3tewna` ← `ديالنا و كيعطينا`; and a 17-against-18 run containing
  `18 tal 25 chher` ← `تمنطاش حتى لخمسة وعشرين شهر`.
- **test-2 split** — `fa houa 3ibara 3an acide hyaluronique` ← `فهو عبارة عن حمض
  هيالورونيك`. **merge** — a 21-against-25 run containing `6 tal 8 achhor` ←
  `ستة تال ثمانية أشهر`.
- **test-3 splits** — `fa hadi` ← `فهذه`; `pigmentés fa houa kay3ti` ←
  `pigmentées، فهو كيعطاو`. **merge** — `kat7taji 4 dial l7essass mabin 7essa
  w7essa` ← `كتحتاجي أربعة ديال الحصص، ما بين حصة وحصة`.
- **vitasilk splits** — `5 d9ay9 eyyh a lalla 5` ← `خمس دقائق. أي هذا 五`;
  **`mn ghir anno il nourrit il hydrate fih 26 vitamines` ← `من غير أنه ينغى,
  يهدئ. فيه ستة وعشرين vitamin`**; `chno katsnay bach thllay f ch3rk siri
  la9rab` ← `شنو كتسني باش تهلي شعرك؟ ديري الأقرب`. **merge** — `matrddadich wla
  d9i9a` ← `ما تردديش ولا دقيقة`.

**`vitasilk` `w0031`–`w0036` needs both directions inside one span.** Taking the
user's correspondence as given:

```
il + nourrit  <- ينغى           one token, two words   (split)
il + hydrate  <- يهدئ           one token, two words   (split)
fih           <- فيه            one to one
26            <- ستة + وعشرين   two tokens, one word   (merge)
```

Six corrected words against five draft tokens, requiring a split **and** a merge
in the same span. No substitution cost can express either; this is Block 9 work
at the earliest and was not opened.

**Do splits correlate with code-switching? No, and the numbers say so.** French
is **16.9%** of the words inside split runs (12 of 71) against **21.3%** of the
corpus (73 of 343 — the corpus is darija 213, fr 73, msa 45, en 9, mixed 3). If
anything the association is slightly negative.

The `il nourrit / il hydrate` case is real and is French, but most split runs
are **Darija proclitic morphology**: `فهو` → `fa houa`, `فهذه` → `fa hadi`,
`دالحلول` → `dial l7loul`, `الفيديو` → `la vidéo` — the correction pass
separating a fused Arabic token into two Arabizi words under ORTHOGRAPHY_GUIDE
§2's attachment rules. **Caveat on the measure**: it counts every word inside an
uneven run, not the extra word itself, which cannot be identified without the
true correspondence. A sharper measure needs the thing being measured.

**Is `26`'s lost anchor the only merge regression? Yes — but it is one of nine
anchor losses.** Comparing every row under both models across all five reels:

- **lost an anchor (9)**: ground-truth `tal`, `dial`, `wa7d`, `dial`; test-1
  `dial`, `la`; vitasilk `eyyh`, **`26`**, `f`
- **gained an anchor (9)**: ground-truth `pigmentés`, `3ndi`, `li`, `houa`;
  test-1 `tb3i`, `m3aya`; vitasilk `5`, `mn`, `chno`

A net wash, consistent with 330 → 330. Of the three losses on `vitasilk` — the
only reel with a reference — the user judged `eyyh` and `f` **wrong** in their
new state, so neither is a regression against human judgement. The six on
`ground-truth` and `test-1` are unjudged, no reference existing for those reels.

**The `26` regression, stated plainly.** Under the legacy model `w0036` held
`وعشرين`; under the adopted model it holds nothing and is interpolated. **Part
1's "zero regressions" did not count it** — the scorer's regression bucket is
rows marked `correct` or `misheard` that moved, and `26` was marked
`two-tokens`, which buckets as *still inexpressible*. The row still got worse,
and the record now says so. In the second pass the user left it **unjudged**,
which is the honest answer for a row whose correct pairing the aligner cannot
express.

### Goal 6 — the generated script is pinned

`core/src/align-sheet.syntax.test.ts`, committed on its own. It parses the
emitted script with `new Function` over the sparse re-review row shape and over
a row set carrying single and double quotes, backslashes, real newlines, tabs,
a literal `</script>`, template-literal syntax and Arabic in both fields.

**It found two defects beyond the one it was written for.** A `</script>` in any
word closed the script element, and U+2028 and U+2029 are legal inside a JSON
string but terminate a line in JavaScript — either would have shipped a sheet
whose script did not parse. Values embedded in the script block go through a
`scriptJson` helper now that escapes `<`, `>`, `&` and both line separators,
rather than bare `JSON.stringify`.

No shipped sheet was affected: the corpus contains no `<` and no line
separator.

### Goal 7 — the record

- **`docs/DEFECT-alignment-script-mismatch.md`**: status changed from "open" to
  fixed-for-the-tie/open-for-splits-and-merges; new **§A.0** with the adoption,
  the user's 17 verdicts dated 2026-08-28, and the corpus table; **§A.0.1** with
  the `26` regression and the nine-lost/nine-gained accounting; **§A.5** with
  the splits-and-merges measurement, the token texts and the code-switching
  answer. Every figure stamped with reel, entry id, prompt version and sha.
  **Nothing was moved to §B** — nothing in it is superseded.
- **`docs/DECISION-transcription-config.md`**: an amendment dated 2026-08-28
  recording that the alignment method named in the freeze now uses a
  transliteration-aware substitution cost, with the evidence, the corpus check,
  the one regression, and that `legacy` stays selectable. References extended.
- **`CLAUDE.md`**: a new convention for the cost model, and a session section.

### Regression check

`npm run check` — **exit 0, `check: PASS`**.

| workspace | tests | against session 11 |
|---|---:|---|
| `@framopia/core` | 342 (20 files) | 337 |
| `framopia-service` | 765 (55 files) | 761 |
| `framopia-benchmarks` | 166 (16 files) | 166 |
| `framopia-panel` | 66 + 2 skipped (3 files) | 66 + 2 |
| **TS total** | **1339** | **1330** |
| pytest (sidecar) | **141** | 141 unchanged |

+5 in core (the syntax test), +4 in service (the cost-model tests).

## Deviations

- **The reference was in `~/Downloads`, not the repo.** The prompt said the user
  had moved it in; he had not. Found it under the name the sheet gave it and
  copied it to the canonical path.
- **`core/src/align-sheet.ts` was changed in Goal 6**, which is a behavioural
  change to the generator in a session whose only sanctioned behavioural change
  is Goal 3. It is not alignment — the constraint is about not bundling two
  alignment experiments — and the test would otherwise have been committed
  failing. It is in its own commit.
- **The migration flaw found in Goal 2(c) was not fixed.** Naming it is what
  the goal asked for; fixing it is a behavioural change nobody sanctioned, and
  the user has already redone the work it would have saved.

## Failures & open problems

- **Nothing was lost or corrupted this session.** No cache entry, ledger line,
  template or hand-made reference was modified. The ledger is byte-identical.
- **`26` is a live regression.** Its true source is two draft tokens and the
  aligner has no operation for that. It is recorded in both documents rather
  than netted away, and it is unjudged in the reference.
- **The sheet's persistence key embeds the git HEAD**, so marks do not survive
  regenerating the sheet. This is why session 11's migration found nothing, and
  it will cost the next reviewer their work the same way unless the key changes.
  **Not fixed.**
- **The split/merge measure is coarse.** It counts every word inside an uneven
  run rather than the extra word, which cannot be identified without the true
  correspondence — the thing being measured.
- **Only `vitasilk` has a human reference.** The adoption moved 67 rows across
  the corpus and 50 of them are on reels nobody has judged. The corpus check
  bounds the damage structurally; it does not confirm the pairings are right.
- **Splits are unimplementable today**, as merges are. Ten split runs and six
  merge runs; Block 9 at the earliest.
- Carried forward: headless AE is not met, `vitasilk` is the only reel ever
  built, the CJK `五` is classified Latin, 28 cards have a clipped hold.

## Repo state

- Branch **`main`**, worked on `main`. No force-push, no history rewritten.
- HEAD at the time of writing: **`5aed86e` `docs: record the adoption and what
  it cost`**, preceded by `test: parse the generated sheet script`,
  `feat: adopt the transliteration-aware substitution cost`, and `docs: add the
  hand-made re-review reference for vitasilk`, on session 11's `127162a`.
  **This report's own commit (`docs: record block 8 session 12`) follows it**
  and is not reflected in that subject line.
- `git log` checked for AI attribution and co-author trailers: none.
- `npm run check`: **exit 0, `check: PASS`** — 1339 TS tests across four
  workspaces, 141 pytest.
- Ledger `.local/costs.jsonl`: **108 lines**, sha256
  `50ec3f57bacb3f32054ae190d3a7652dea5e55c68d71a494fa85b2347a417593` at start
  and end. All-time spend unchanged at **$10.968590**.
- After Effects: **1 instance**, **0** `aerender`. Not driven.

## Suggested next step

Re-run the five plans through the adopted aligner and look at what moved, since
67 rows changed anchor and only the 17 on `vitasilk` have been judged. The
plans on disk still carry legacy timings; regenerating them is free — everything
is cached — and it is what puts the fix in front of the builder rather than only
in the scorer. `test-1` is the reel to judge next if a second reference is ever
worth the hour: it has 17 moved rows, the most after `vitasilk`, and it is the
only other reel with keywords and image slots planned. After that the open
question is no longer the cross-script tie but splits and merges, which need an
operation the aligner does not have.

## What the user does next

Nothing is needed from you for the adoption — it is in, and the evidence is your
two reference files.

**Your original seventeen marks are probably still in your browser.** Session 11
told you they would be restored and they were not; the reason is that the sheet
remembers marks under a key that includes the exact version of the code it was
built from, and regenerating the sheet changed that version. They are under the
older key, from the sheet you first marked. You have already redone the work, so
this costs you nothing now — but it is why the restore did not happen, and I was
wrong to tell you it would.

**What changed in the aligner.** The pairing you judged is now the default. Your
seven "correct" and two "misheard" verdicts are the evidence for it, and none of
the fifty-four rows you had confirmed earlier moved.

**One row got worse and I want you to know rather than find out later.** `26`
used to be attached to `وعشرين`; it is now attached to nothing. That is because
`26` is really two spoken words, `ستة` and `وعشرين`, and the aligner still has no
way to say "one word, two tokens" — which is exactly why you left that row
unjudged. It is the only row of its kind that got worse, and it is written into
both the defect record and the transcription decision.

**The same limit explains `il nourrit, il hydrate`.** You identified it: those
are French, Scribe writes only Arabic, and it collapsed each pair into one word.
The aligner cannot split one token across two words any more than it can merge
two into one. Across all five reels there are ten places needing a split and six
needing a merge — more than the "one" the earlier estimate gave, and in the
opposite direction. That is the next real piece of work, and it is not something
this session could have done.
