[pytest]
# The package lives at the rootdir, not on sys.path: the sidecar is invoked as
# `python -m framopia_cv.cli` from this directory and is deliberately not
# installed into the venv.
pythonpath = .
testpaths = tests
